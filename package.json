{
  "name": "rsbuild-vanilla-js",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "^0.1.5",
    "purescript": "^0.15.13",
    "spago": "^0.21.0"
  },
  "dependencies": {
    "@node-frp/core": "^0.0.5"
  }
}
